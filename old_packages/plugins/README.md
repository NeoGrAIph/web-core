# `packages/plugins`

Здесь живут внутренние плагины Payload (workspace пакеты).

Правила:
- плагины должны быть максимально изолированными и переиспользуемыми между apps;
- любые изменения, влияющие на схему (collections/globals/hooks), требуют проверки миграций.

