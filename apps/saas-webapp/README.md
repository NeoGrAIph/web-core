# `apps/saas-webapp` (placeholder)

SaaS веб‑интерфейс (reputation management).

Здесь появится реальный Next.js app, когда:
- определим auth/roles/permissions и интеграцию с платформой (Keycloak и т.п. — зона `synestra-platform`),
- решим структуру доменной логики и общих пакетов (возможно, появятся `packages/auth`, `packages/db`, `packages/observability`).

До появления `package.json` эта директория — только “маячок” структуры.

