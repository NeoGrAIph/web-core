# apps/synestra-io/src/migrations

## Назначение
Миграции базы данных Payload для конкретного приложения.

## Границы модуля (не окончательные)
- Только миграционные файлы и вспомогательные материалы.
- Генерируются и применяются по канону runbook-payload-migrations.md.

Важно: границы модуля в этом README.md не окончательные и могут быть сужены или расширены
по мере обработки соответствующего модуля из шаблона website.

## Источники (for_cute)
- (не было явных файлов в for_cute; миграции будут создаваться по мере изменений schema)

## Зависимости
- runbook-payload-migrations.md
- payload.config.ts (migrationDir)

## Требования и ограничения
- Не смешивать push и migrate для одной БД.
- В prod миграции обязательны (через GitOps Job).

## Статус
Модуль подготовлен, миграции будут добавляться по мере изменений schema.
