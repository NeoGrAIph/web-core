# ВНИМАНИЕ: не коммитьте реальные значения секретов.

# Non-secret: окружение деплоя (dev|stage|prod). Локально можно не задавать (по умолчанию dev).
SYNESTRA_ENV="dev"

# Secret: ключ Payload (JWT/шифрование). Должен быть стабильным в пределах deployment.
PAYLOAD_SECRET="replace-me"

# Secret: строка подключения к БД (для dev может быть локальная Postgres).
DATABASE_URI="postgresql://user:pass@localhost:5432/web_synestra_io"

# Secret: используется для вызова jobs endpoint (Authorization: Bearer ...), если включим jobs.
CRON_SECRET="replace-me"

# Secret: используется для external share-preview (подписанные ссылки с TTL).
# Стандарт `web-core`: не передавать секреты preview в URL/querystring (токен живёт в `#fragment`).
PREVIEW_SECRET="replace-me"

# Secret: дополнительная защита seed endpoint `POST /next/seed` в stage/prod.
# Рекомендуемый путь: задать `SEED_KEY`, затем выполнить seed вручную и удалить/сбросить ключ.
SEED_KEY="replace-me"

# Non-secret: публичный URL приложения (должен совпадать с ingress host в k8s).
NEXT_PUBLIC_SERVER_URL="http://localhost:3000"
