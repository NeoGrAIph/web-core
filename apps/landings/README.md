# `apps/landings` (placeholder)

Группа лендингов. По принятой стратегии — **один deployment на группу** (на старте).

Здесь появится реальный Next.js app, когда:
- согласуем структуру контентных блоков (`packages/cms-blocks`),
- определим требования к SEO/редиректам/preview (часть приходит из Payload template `website`),
- решим вопрос по UI‑стеку (Tailwind 3 vs 4).

До появления `package.json` эта директория — только “маячок” структуры.

