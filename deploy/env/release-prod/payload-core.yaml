# Release values for payload.services.synestra.tech deployment (prod).

env:
  APP_NAME: "@synestra/payload-core"

image:
  repository: "registry.gitlab.com/synestra/synestra-platform/web-payload-core"
  tag: "13d512c4913a-r1"
  pullSecrets:
    - name: gitlab-regcred
