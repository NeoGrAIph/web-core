apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: web-saas-dev
  namespace: argo
spec:
  project: synestra-web
  source:
    repoURL: https://github.com/NeoGrAIph/web-core.git
    targetRevision: main
    path: deploy/charts/web-app
    helm:
      valueFiles:
        - ../../env/release/saas.yaml
        - ../../env/dev/saas.yaml
  destination:
    server: https://kubernetes.default.svc
    namespace: web-saas-dev
  syncPolicy:
    automated:
      prune: true
      selfHeal: false
    syncOptions:
      - CreateNamespace=true
